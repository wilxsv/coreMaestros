{% extends 'MaestroCoreBundle::default.html.twig' %}
{% block title %}Situación legal{% endblock %}
{% block body %}
    <table id="sample-table" class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Tiposituacion</th>
                <th>Operaciones</th>
            </tr>
        </thead>
        <tbody>
        {% for ctlSituacionLegal in ctlSituacionLegals %}
            <tr>
                <td>{{ ctlSituacionLegal.tipoSituacion }}</td>
                <td>
                  <a href="{{ path('situacionlegal_show', { 'id': ctlSituacionLegal.id }) }}">Ver <i class="icon-show bigger-120"></i></a>
                  <a href="{{ path('situacionlegal_edit', { 'id': ctlSituacionLegal.id }) }}">Editar <i class="icon-edit bigger-120"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

<a href="{{ path('situacionlegal_new') }}">Agregar Situación legal</a>
{% endblock %}
{% block footer_js %}<script type="text/javascript">$(function(){ var oTable2 = $('#sample-table').dataTable(); })</script>{% endblock %}
