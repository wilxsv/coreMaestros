{% extends 'MaestroCoreBundle::default.html.twig' %}
{% block title %}Codificación ::  {{ ctlCodificacion.nombreCodificacion }}{% endblock %}
{% block body %}
    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ ctlCodificacion.nombreCodificacion }}</td>
            </tr>
            <tr>
                <th>Metodo</th>
                <td>{{ ctlCodificacion.metodoCodificacion }}</td>
            </tr>
        </tbody>
    </table>

<a href="{{ path('codificacion_index') }}" class="btn">Regresar a listado</a>
<a href="{{ path('codificacion_edit', { 'id': ctlCodificacion.id }) }}" class="btn btn-primary">Editar</a>
    {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar" class="btn btn-xs btn-danger" />
            {{ form_end(delete_form) }}
{% endblock %}
