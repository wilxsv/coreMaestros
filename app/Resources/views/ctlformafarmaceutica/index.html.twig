{% extends 'MaestroCoreBundle::default.html.twig' %}
{% block title %}Formas Fármaceuticas{% endblock %}
{% block body %}
<div class="row-fluid">
    <table id="sample-table" class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Operaciones</th>
            </tr>
        </thead>
        <tbody>
          {% for ctlFormaFarmaceutica in ctlFormaFarmaceuticas %}
                    <tr>
                          <td>{{ ctlFormaFarmaceutica.nombreFormaFarmaceutica }}</td>
                          <td>
                           <a href="{{ path('formafarmaceutica_show', { 'id': ctlFormaFarmaceutica.id }) }}">Ver <i class="icon-show bigger-120"></i></a>
                           <a href="{{ path('formafarmaceutica_edit', { 'id': ctlFormaFarmaceutica.id }) }}">Editar <i class="icon-edit bigger-120"></i></a>
                          </td>
                    </tr>
          {% endfor %}
        </tbody>
    </table>
</div>
        <br />
        <a href="{{ path('formafarmaceutica_new') }}" class="btn btn-small btn-primary">Agregar Forma Fármaceutica</a>
{% endblock %}
{% block footer_js %}<script type="text/javascript">$(function(){ var oTable2 = $('#sample-table').dataTable(); })</script>{% endblock %}
