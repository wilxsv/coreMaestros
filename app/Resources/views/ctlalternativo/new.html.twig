{% extends 'MaestroCoreBundle::default.html.twig' %}
{% block title %}Registrar nuevo medicamento alternativo{% endblock %}
{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Agregar" class="btn btn-primary" />
    {{ form_end(form) }}

<a href="{{ path('alternativo_index') }}" class="btn">Regresar a listado</a>
{% endblock %}
