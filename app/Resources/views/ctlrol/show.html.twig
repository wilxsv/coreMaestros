{% extends 'base.html.twig' %}
{% block title %}Rol [ {{ ctlRol.nombreRol }} ]{% endblock %}
{% block body %}
    <table>
        <tbody>
            <tr>
                <th>Nombre de rol</th>
                <td>{{ ctlRol.nombreRol }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_roles_index') }}" class="btn">Regresar a listado</a>
    <a href="{{ path('admin_roles_edit', { 'id': ctlRol.id }) }}" class="btn">Editar</a>
    {{ form_start(delete_form) }}<input type="submit" value="Eliminar rol" class="btn btn-xs btn-danger" />{{ form_end(delete_form) }}
{% endblock %}
