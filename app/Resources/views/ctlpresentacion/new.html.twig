{% extends 'MaestroCoreBundle::default.html.twig' %}
{% block title %}Registrar presentación{% endblock %}
{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('presentacion_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
