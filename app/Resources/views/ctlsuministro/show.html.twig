{% extends 'MaestroCoreBundle::default.html.twig' %}
{% block title %}Tipo Suministro {% endblock %}
{% block body %}
    <table>
        <tbody>
            <tr>
                <th>Nombresuministro</th>
                <td>{{ ctlSuministro.nombreSuministro }}</td>
            </tr>
            <tr>
                <th>Codificación</th>
                <td>{{ ctlSuministro.codificacionSuministro }}</td>
            </tr>
            <tr>
                <th>Quien lo solicita</th>
                <td>{{ ctlSuministro.rolSolicitaSuministro }}</td>
            </tr>
            <tr>
                <th>Quien lo valida</th>
                <td>{{ ctlSuministro.rolSolicitaSuministro }}</td>
            </tr>
        </tbody>
    </table>

<a href="{{ path('suministro_index') }}" class="btn">Regresar a listado</a>
<a href="{{ path('suministro_edit', { 'id': ctlSuministro.id }) }}" class="btn btn-primary">Editar</a>
    {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar Suministro" class="btn btn-xs btn-danger" />
            {{ form_end(delete_form) }}
{% endblock %}
